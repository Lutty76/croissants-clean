{% extends 'persoCroissantBundle::base.html.twig' %}

{% block title %}List des utilisateurs{% endblock %}

{% block body %}
<h3>Liste des participants</h3>
<table><tr><th>ID</th><th> Google ID</th><th>Nom</th><th>Email</th><th>Coefficient</th><th>Don</th><th>Joker</th></tr>
     {% for item in users %}

               <tr> <td> <a href="{{ path('_profil',{ id :  item.id  }) }}">{{ item.id }} </a> </td><td> <a href="{{ path('_profil',{ id :  item.id  }) }}">{{ item.googleId }} </a> </td><td><a href="{{ path('_profil',{ id :  item.id  }) }}"> {{ item.username }}</a>  </td><td> <a href="{{ path('_profil',{ id :  item.id  }) }}">{{ item.email }} </a> </td><td>  {{ item.coefficient }}  </td><td>  {% if item.premium == 0 %}
   Non
{% else %}
    Oui
{% endif  %}  </td><td> {{ item.joker }}</td></tr>

            {% endfor %}
</table>
{% endblock %}