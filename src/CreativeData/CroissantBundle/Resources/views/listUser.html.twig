{% extends 'CreativeDataCroissantBundle::base.html.twig' %}

{% block title %}Liste des utilisateurs{% endblock %}

{% block body %}
<h3>Liste des participants</h3>
<table><tr><th>ID</th><th> Google ID</th><th>Nom</th><th>Email</th><th>Coefficient</th><th>Selection</th><th>Joker</th></tr>
     {% for item in users %}

               <tr> 
                   <td> <a href="{{ path('_profil',{ id :  item.id  }) }}">{{ item.id }}        </a> </td>
                   <td> <a href="{{ path('_profil',{ id :  item.id  }) }}">{{ item.googleId }}  </a> </td>
                   <td> <a href="{{ path('_profil',{ id :  item.id  }) }}"> {{ item.username }} </a> </td>
                   <td> <a href="{{ path('_profil',{ id :  item.id  }) }}">{{ item.email }}     </a> </td>
                   <td> {{ item.coefficient }}                                                       </td>
                   <td> {{ selection[item.username][1] }}                                               </td>
                   <td> {{ item.joker }}                                                             </td>
               </tr>

            {% endfor %}
</table>
{% endblock %}